# service-menu-dialog
It's a script written in Bash for Dialog to check your SystemServices

to download:

sudo curl -L https://raw.githubusercontent.com/bako2010/service-menu-dialog/main/service-menu.sh -o /usr/local/bin/service-menu && sudo chmod +x /usr/local/bin/service-menu


to start it: 

service-menu
